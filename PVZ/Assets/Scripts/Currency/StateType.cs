public enum PlantState
{
    Idle,
    Shoot,
    Die,
}

public enum ZombieState
{
    Move,
    Eat,
    Die,
}

public enum ZombieType
{
    Normal,
}

public enum UIType
{
    ChoosePlant,
    FightUI,
    
}